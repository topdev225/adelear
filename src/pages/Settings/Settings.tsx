import React from 'react';
import {
    PageContainer
} from '@Components';

export const Settings = () => {
    return (
        <PageContainer header={'Admin'}>
            <div>Settings/Admin</div>
        </PageContainer>
    )
};

Settings.displayName= 'Settings';
